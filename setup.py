from setuptools import setup

setup(
    name='quiz-verse',
    version='1.0',
    description='A customtkinter-powered quiz app',
    author='Yugank',
    install_requires=['customtkinter', 'requests'],
    py_modules=['main'],
)
